// Deklarasi variabel x dan y
let x = 10; // Misalkan x = 10
let y = 5;  // Misalkan y = 5

// Membuat kondisi dengan if
if (x > y) {
    console.log("Horee saya bisa!"); // Pesan yang muncul jika x lebih besar dari y
}
